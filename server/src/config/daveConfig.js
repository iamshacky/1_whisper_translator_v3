export const appConfig = {
  USE_LANGUAGE_DETECTION: true,   // Set to false to hardcode 'en'
  ENABLE_TTS: true,               // You can use this later
  ENABLE_MODERATOR: true          // You can wire this into /moderate-message later
};
